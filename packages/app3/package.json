{
  "name": "app3",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "yarn run clean && webpack --mode production --env.PRODUCTION --config ../../webpack.config.js",
    "start": "yarn run webpack-dev-server --port 8082 --mode development --env.DEVELOPMENT --open --config ../../webpack.config.js",
    "clean": "rimraf dist",
    "analyze-bundles": "yarn run clean && webpack --config ../../webpack.config.js --env.ANALYSE_BUNDLES=yes --env.ANALYZER_PORT 8890"
  },
  "dependencies": {
    "@types/lodash": "^4.14.134"
  }
}
